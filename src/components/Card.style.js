import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#32B444', // Cambiar a tu color verde deseado
        borderWidth: 1, // Grosor del borde
        borderColor: '#32B444', // Color del borde (coincide con el fondo para hacerlo invisible)
        borderRadius: 10, // Radio de borde para hacer esquinas redondeadas
        padding: 10, // Espacio interior para el contenido
        fontSize: 25,
    },
});
